def save_prediction(
    connection,
    prediction_name,
    forecast_time,
    api_response,
):
    """
    Persist an API prediction response into SQL Server.

    SQL implementation will be added after the API response
    schema and destination SQL table are confirmed.
    """

    raise NotImplementedError("Prediction result storage is not configured yet.")
